<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        tools:showIn="navigation_view"
        android:theme="@style/AppTheme.ActionBar"
        android:orderInCategory="5">

    <group android:checkableBehavior="single">

    <item
            android:id="@+id/home_fragment"
            android:icon="@drawable/ic_home"
            android:orderInCategory="1"
            app:labelVisibilityMode="labeled"
            android:title="@string/navHome" />

    <item
            android:id="@+id/searchRoutine_fragment"
            android:icon="@drawable/ic_routine"
            android:orderInCategory="2"
            app:labelVisibilityMode="labeled"
            android:title="@string/navRoutines" />

    <item
            android:id="@+id/profile_fragment"
            android:icon="@drawable/ic_profile"
            android:orderInCategory="3"
            app:labelVisibilityMode="labeled"
            android:title="My Profile" />

    <item
            android:id="@+id/favourite_fragment"
            android:icon="@drawable/ic_fav"
            android:orderInCategory="4"
            app:labelVisibilityMode="labeled"
            android:title="@string/navFav" />
    <item
            android:id="@+id/settings_fragment"
            android:icon="@drawable/ic_setting"
            android:orderInCategory="5"
            app:labelVisibilityMode="labeled"
            android:title="@string/navSetting" />
    </group>

</menu>