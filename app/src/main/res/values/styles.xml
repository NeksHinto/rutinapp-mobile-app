<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="SwitchButton" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- active thumb & track color (30% transparency) -->
        <item name="colorControlActivated">@color/switchActivated</item>

        <!-- inactive thumb color -->
        <item name="colorSwitchThumbNormal">@color/switchNormal</item>

        <!-- inactive track color (30% transparency) -->
        <item name="android:colorForeground">@color/switchForeground</item>
    </style>
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:statusBarColor">@color/green_menu</item>
        <item name="windowActionBar">false</item>

        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.ActionBar" parent="ThemeOverlay.AppCompat.ActionBar">
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:actionMenuTextColor">@color/black</item>
        <item name="color">@android:color/transparent</item>
        <item name="background">@color/green_app</item>
        <item name="android:activatedBackgroundIndicator">@drawable/drawer_list_selector</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <style name="AppTheme.DrawerHeaderBar">
        <item name="background">@color/green_menu</item>
    </style>
</resources>